# MaunPrekshak CLI Environment Variables (Optional)
# Copy to .env if you wish to use Gemini AI summaries locally

# Google Gemini API Key (optional — get from https://aistudio.google.com/app/apikey)
# If omitted, run with `mp scan . --no-ai`
GEMINI_API_KEY=your_gemini_api_key_here
